<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/shape_item"
    android:layout_width="match_parent"
    android:paddingBottom="18dp"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/imgItemGrid"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginStart="22dp"
        android:layout_marginTop="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_launcher_background" />

    <TextView
        android:id="@+id/tv_name_item_grid"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="23dp"
        android:fontFamily="@font/open_sans"
        android:text="TextView"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="@+id/imgItemGrid"
        app:layout_constraintTop_toBottomOf="@+id/imgItemGrid" />

    <TextView
        android:id="@+id/tv_num_subj"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:fontFamily="@font/open_sans"
        android:text="TextView"
        android:textColor="#D9D9D9"
        android:textSize="12sp"
        app:layout_constraintStart_toStartOf="@+id/tv_name_item_grid"
        app:layout_constraintTop_toBottomOf="@+id/tv_name_item_grid" />
</androidx.constraintlayout.widget.ConstraintLayout>